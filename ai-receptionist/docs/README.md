# Platform knowledge cache

One file per platform or business type: `<platform-or-type>.md`.
Each starts with `Last verified: YYYY-MM-DD`.
Reusable knowledge only — no client-specific facts (those live in `clients/`).
